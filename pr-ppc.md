## G6 — Export PPC seeds (CSV) on the Intelligence tab

One click → a **Sponsored Products exact-match seed list** built entirely client-side from the keywords already on screen. **No new endpoint, no Jungle Scout credits** — pure LEAN.

- **Prioritization** (the sort, top rows = launch first): proven **keyword sales** dominate, search volume assists, and a **+bonus for title density ≤ 2** — almost nobody carries the phrase in their title, which is exactly where exact-match clicks are winnable and typically cheaper.
- **Columns**: Keyword, Match Type (Exact), Search Volume, Keyword Sales, Opportunity, Title Density, Our Rank, Action — paste the top rows into a Sponsored Products campaign; the metric columns are there for your bid judgment.
- **No silent drops**: all CRITICAL/UPGRADE/REINFORCE keywords are included — sorting IS the prioritization. (DEFENDED/OPTIMIZED are excluded: you already rank; exact-match ads there mostly cannibalize organic clicks.)
- Note: keywords with competitor brand names (e.g. "…sandisk…") **are** included — they're banned from listing content, but bidding on competitor terms in ads is a legitimate, allowed conquesting tactic. Your call to use or trim.

`tsc` exit 0. UI-only, one file, no migration.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
